# Desktop (local-first) culler. torch + torchvision come from the CPU wheel index (matched pair):
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# vs. the hosted app this DROPS: psycopg, pgvector, selectolax, httpx (no Postgres, no Gallera).
# Storage is stdlib sqlite3; ingest is local files.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# ML pipeline (reused verbatim from pipeline/) — exact pins (ABI-sensitive).
torch==2.12.0
torchvision==0.27.0
open_clip_torch==3.3.0
opencv-python-headless==4.13.0.92
numpy==2.4.6
pillow==11.1.0

# RAW preview-first (embedded-JPEG extraction + fallback decode) — required for the desktop pivot.
rawpy==0.27.0

# dev/test
pytest==8.2.2
httpx==0.27.0   # FastAPI TestClient dependency
