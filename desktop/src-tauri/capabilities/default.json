{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Folder dialog + sidecar server spawn + auto-update for the main window. The SPA is served by the bundled sidecar at http://127.0.0.1:8756 — a REMOTE origin from Tauri's view — so the capability must opt that origin in (`remote.urls`), otherwise the dialog IPC (folder picker) is denied and pickFolder() silently no-ops.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:8756/*", "http://localhost:8756/*"]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default",
    "shell:allow-spawn",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "cull-server", "sidecar": true }]
    }
  ]
}
