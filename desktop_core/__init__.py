"""Photo Cherrypick — desktop (local-first) core.

Reuses the analysis pipeline from `pipeline/` verbatim; adds local-folder ingest, preview-first
RAW loading, a SQLite store, and editor-agnostic export. See docs/ for the Phase-1 PRD.
"""
__version__ = "0.1.0"
